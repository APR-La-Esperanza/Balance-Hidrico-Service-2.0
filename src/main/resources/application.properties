server.port=${SERVER_PORT:8088}
spring.application.name=BalanceHidrico_Service

# CONFIGURACIÓN DE BASE DE DATOS (NEON)
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-jolly-field-aqon4mca-pooler.c-8.us-east-1.aws.neon.tech/apr_balance_db?sslmode=require&channel_binding=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_Cty1ZxAT0kzb}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# CONFIGURACIÓN DE SWAGGER / OPENAPI (SPRING BOOT 3+)
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.packages-to-scan=com.apr.BalanceHidrico_Service.controller

# CONFIGURACIÓN DE EUREKA
eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8761/eureka}
eureka.instance.prefer-ip-address=true
