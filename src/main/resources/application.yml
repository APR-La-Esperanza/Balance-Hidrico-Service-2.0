server:
  port: ${SERVER_PORT:8088}

spring:
  application:
    name: balancehidrico-service

  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-jolly-field-aqon4mca-pooler.c-8.us-east-1.aws.neon.tech/apr_balance_db?sslmode=require&channel_binding=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_Cty1ZxAT0kzb}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
